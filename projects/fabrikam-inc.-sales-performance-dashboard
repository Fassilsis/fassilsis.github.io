<div class="project-detail">

  <h1>{{ project.title }}</h1>

  <p>{{ project.description }}</p>

  <section class="screenshots">
    {% for image in project.screenshots %}
      <img src="{{ image }}" alt="Screenshot for {{ project.title }}" />
    {% endfor %}
  </section>

  <section class="readme-content">
    <!-- Rendered markdown content goes here -->
    {{ project.readme_html }}
  </section>

  <section class="tools-used">
    <h3>Tools Used</h3>
    <ul>
      {% for tool in project.tools %}
        <li>{{ tool }}</li>
      {% endfor %}
    </ul>
  </section>

  <a href="{{ project.github }}" target="_blank" class="github-link">🔗 View Project on GitHub</a>

</div>
